services:
  - type: web
    name: aerostudy-api
    env: python
    buildCommand: cd aerostudy-backend && pip install -r requirements.txt --no-cache-dir
    startCommand: cd aerostudy-backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    pythonVersion: "3.11.0"
